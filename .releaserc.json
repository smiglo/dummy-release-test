{
  "branches": [
    "main",
    {
      "name": "next",
      "prerelease": "next",
      "channel": "next"
    },
    {
      "name": "+([0-9])\\.+([0-9])\\.x-maintenance",
      "range": "${name}",
      "channel": "${name}"
    }
  ],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    "@semantic-release/changelog",
    [
      "@semantic-release/exec",
      {
        "prepareCmd": "./.github/tools/mk-release-package.sh ${nextRelease.version}"
      }
    ],
    [
      "@semantic-release/git",
      {
        "assets": ["CHANGELOG.md"],
        "message": "chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
      }
    ],
    [
      "@semantic-release/github",
      {
        "assets": [
          { "path": "build/src/hello_world", "label": "hello_world (Linux Binary)" },
          { "path": "build/hello_world-*.tar.gz", "label": "Source Code (tar.gz)" }
        ]
      }
    ]
  ]
}
